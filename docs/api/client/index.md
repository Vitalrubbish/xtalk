# Client API

This page is generated from the frontend TypeScript sources.

From the repository root:

```bash
cd frontend
npm install
npm run docs
```

The generated output is written to `docs/api/client/`.
